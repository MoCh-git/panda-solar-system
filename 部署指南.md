# 《爱宝的太阳系之旅》部署与更新指南

> 本指南面向零基础用户。发布由 ZCode 帮你完成，这里说明原理和手动操作的方法。

---

## 一、这个网站是什么？

- 位置：`D:\ZCode work\panda-solar-system\`
- 类型：纯静态网页（HTML + CSS + JavaScript），不需要服务器和数据库
- 手机、平板、电脑都能用；星星进度自动保存在各自的浏览器里

## 二、文件说明

| 文件 | 作用 |
|---|---|
| `index.html` | 网站入口（双击它或用 启动.bat 打开） |
| `css\style.css` | 全部外观样式（星空、星球、爱宝、卡片） |
| `js\data-space.js` | 太空内容：太阳 + 八大行星 + 月球知识卡 |
| `js\data-earth.js` | 地球内容：七大洲 → 国家 → 城市 → 景点 → 历史故事 → 测验题 |
| `js\app.js` | 交互逻辑（页面切换、答题、星星进度） |
| `启动.bat` | 双击后在浏览器里本地打开网站 |
| `本机服务器.ps1` | 本机/手机局域网访问用的临时服务器（测试用，发布后可不用） |

**以后想加内容**（新国家、新景点、新故事）：直接告诉 ZCode，改 `js\data-earth.js` 就行。

## 三、本地预览的两种方式

1. **最简单**：双击 `启动.bat`（直接打开文件，功能完整）
2. **带手机访问**：双击运行 `本机服务器.ps1`（右键 → 使用 PowerShell 运行），
   手机连同一 WiFi，浏览器输入窗口里显示的地址（如 `http://192.168.x.x:8388/`）

## 四、已安装的工具（都在 D 盘）

| 工具 | 位置 | 用途 |
|---|---|---|
| Git for Windows | `D:\Git` | 把网站文件传送到 GitHub |
| GitHub CLI | `D:\GitHubCLI` | 登录 GitHub、创建仓库、开启网页托管 |
| 安装包备份 | `D:\GitHub安装包` | 确认一切正常后可以删除 |

两个工具已加入系统环境变量（新开终端直接可用 `git` 和 `gh` 命令）。

## 五、发布到互联网的原理（ZCode 已代办）

1. 在 GitHub 创建公开仓库 `panda-solar-system`
2. 把网站文件推送到仓库
3. 开启 GitHub Pages（免费静态网页托管）
4. 得到网址：`https://你的用户名.github.io/panda-solar-system/`

## 六、以后怎么更新网站？

直接告诉 ZCode，例如：
- "给亚洲加上韩国和首尔的景福宫故事"
- "把测验题换难一点"

ZCode 会修改文件并自动重新发布，一分钟后刷新网页即可看到。

## 七、想手动发布/更新？（可选，了解即可）

前提：已在终端执行过 `gh auth login` 登录。

```
cd /d "D:\ZCode work\panda-solar-system"
git add .
git commit -m "更新内容"
git push
```

推送后等 1-2 分钟，GitHub Pages 自动生效。

## 八、常见问题

**Q：网址打不开？**
A：GitHub Pages 首次开启或刚推送后需要等 1-2 分钟；再检查网络（需要能访问 github.io）。

**Q：星星进度换了设备就没了？**
A：进度存在浏览器本地（localStorage），换设备/换浏览器会重新开始，这是正常设计。

**Q：想改网站名字或换熊猫形象？**
A：告诉 ZCode 即可。
